import { createMuiTheme } from '@material-ui/core/styles';

const customTheme = {
  props: {
    // MuiButtonBase: {
    //   disableRipple: true,
    // },
  },
};

export default createMuiTheme(customTheme);
